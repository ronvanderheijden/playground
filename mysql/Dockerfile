ARG MYSQL_VERSION=latest
FROM mariadb:${MYSQL_VERSION}

ARG MYSQL_ROOT_PASSWORD
ARG MYSQL_DATABASE
ARG MYSQL_USER
ARG MYSQL_PASSWORD
ARG MARIADB_ALLOW_EMPTY_ROOT_PASSWORD

ENV DEBIAN_FRONTEND noninteractive

COPY init /docker-entrypoint-initdb.d
