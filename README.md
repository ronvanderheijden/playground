# Playground
Just a small application that uses nginx, php and a mysql database.

```sh
# copy env file
cp -n .env{.example,}
```

```sh
# start containers
docker-compose up --detach
```

```sh
# test
curl localhost
```
